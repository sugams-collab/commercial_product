Frontend (React + Vite)
========================

Prereqs: Node 18+, pnpm/npm/yarn

Install & run (dev):

```bash
cd frontend
npm install
npm run dev
```

By default Vite serves on `http://localhost:5173`. The frontend expects backend API at `VITE_API_URL` (defaults to `http://127.0.0.1:8000`).
